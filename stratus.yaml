stacks:
  - name: aws-bedrock-artefacts-{{env:ENVIRONMENT}}

    capabilities: []
    parameters:
      - key: Environment
        value: '{{env:ENVIRONMENT}}'
    terminationProtection: true

    policyFile: ./policy.json
    templateFile: ./components/artefacts.yaml
